version: '3.8'

services:
  netmapper-api:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000
    environment:
      - SHODAN_API_KEY=${SHODAN_API_KEY}
